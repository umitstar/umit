1978年美国电话电报公司发布C语言，ANSI C（American National Standards Institute),UNIX系统用 C语言优秀的程序设计语言。
MS C， Turbo C，AT & TC。
ANSI C 32:= auto,break,case,char,const, continue, default, do , double, else, enum, extern, float, for,goto, if , int, long, register,return, short, signed, static, sizof, struct, switch, typedef,union, unsigned, void,volatile,while.
Turbo C =  11 个  （asm,_cs, _ds,_es，_ss, cdecl, far,hugeinterrupt,near,pascal.）
运算符=34.
C++最新版本=Borland C++,Symantec C++, Microsoft VisualC++.
【例1.1】
main()
{
   printf("世界，您好！\n");
   }
main 是主函数的函数名 必须有而且一个，
printf函数吧输出的内容送到显示器去显示，可以程序中直接调用。
【例1.2】
#include<math.h>
#include<stdio.h>
main()
  {
     double x,s;
     printf("input number:\n");
     scanf("%lf",&x);
     s=sin(x);
     printf("sine of%lfis%lf\n",x,s);
        }
          scanf是输入函数
          printf是输出函数
          主函数体中又分为两部分，1.说明，2.执行
          说明=变量的类型说明
          scanf格式输入函数
          printf格式函数
          格式控制串=字符串（参数表）必须用双引号括起来（表示数据类型）
          printf("sine of%lf is %lf\n",x,s); 这里%lf为格式字符=双精度浮点数处理
          
          【例1.3】
          int max(int a,int b);          /*函数说明*/
          main()                         /*主函数*/
          {
             int x,y,z;                 /*变量说明*/
             int max(int a,int b);      /*函数说明*/
             printf("input two numbers:\n");
             scanf("%d%d,&x,&y");        /*输入x,y值*/
             z=max(x,y);                 /*调用max函数*/
             printf("maxmum=%d,"z);      /*输出*/
              }

             int max(int a,int b)        /*定义max 函数*/
             {
             if(a>b)returen a;else returen b; /*把结果返回主调函数*/
             }
             需要好好解释
             
          
